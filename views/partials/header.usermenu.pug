.header-usermenu._none
  .header-usermenu__arrow
  header._relative._flex-cc
    .usermenu__avatar-frame._avatar-frame
      img.usermenu__avatar-img._avatar-img(src="{{ .userAvatarUrl }}")
    div
      span.usermenu__username._block #{ .username }
      span.usermenu__email._block #{ .userEmail }
  ul
    li
      a._hover-color-black(href="/new-story") Write a story
    li
      a._hover-color-black(href="/me/stories") Stories
    li
      a._hover-color-black(href="/me/settings") Settings
  footer
    a.usermenu__signout._hover-color-black(href="/signout") Signout
